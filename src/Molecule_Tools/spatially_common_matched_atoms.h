#if ! defined(SPATIALLY_COMMON_MATCHED_H)
#define SPATIALLY_COMMON_MATCHED_H 1

extern int 
spatially_common_matched_atoms(resizable_array_p<Molecule> & molecules,
                        Substructure_Results * sresults);
#endif
